---
description: Reinstall dependencies for the WCAG Mastery Dashboard (cleans node_modules).
---

1. Navigate to the WCAG Dashboard directory.
// turbo
2. Execute `cd /home/puterakahfi/.sov-agent/skills/wcag-mastery/app && rm -rf node_modules package-lock.json && npm install`
3. Notify user that re-installation is complete.
